{
  "name": "redis-async-gen",
  "version": "0.1.0",
  "license": "MIT",
  "main": "dist/redis-async-gen.min.js",
  "scripts": {
    "test": "jest",
    "build": "run-s mkdirs compile",
    "compile": "babel src/index.js --out-file dist/index.js",
    "mkdirs": "shx mkdir -p dist"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@babel/cli": "^7.4.4",
    "@babel/core": "^7.4.5",
    "@babel/preset-env": "^7.4.5",
    "babel-jest": "^24.8.0",
    "find-free-port": "^2.0.0",
    "jest": "^24.8.0",
    "npm-run-all": "^4.1.5",
    "redis-server": "^1.2.2",
    "shx": "^0.3.2"
  },
  "babel": {
    "presets": [
      "@babel/preset-env"
    ]
  },
  "dependencies": {
    "async-redis": "^1.1.7",
    "regenerator-runtime": "^0.13.2"
  }
}
